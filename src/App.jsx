import React,{useMemo,useState}from'react'
import{Upload,FileText,CheckCircle,AlertTriangle,Download,Trash2,Plus,Wand2,ScanText,Loader2,RefreshCw,HelpCircle,Repeat2}from'lucide-react'
import * as pdfjsLib from'pdfjs-dist'
import Tesseract from'tesseract.js'
pdfjsLib.GlobalWorkerOptions.workerSrc=`https://unpkg.com/pdfjs-dist@4.10.38/build/pdf.worker.min.mjs`
const clean=s=>String(s||'').replace(/\s+/g,' ').trim()
const qte=s=>{const m=String(s||'').match(/(\d+(?:[,.]\d+)?)/);return m?Number(m[1].replace(',','.')):0}
const fmt=n=>Number.isInteger(n)?String(n):String(n).replace('.',',')
const units='PCE|PCS|ML|M2|M3|CAR|BTE|BT|ROU|SAC|UN|PAL|PIL|PLA|SEA|KG|L|COL|PAQ'
const articleWords=/(VIS|RONDELLE|ECROU|ÉCROU|POINTE|PENTURE|BUTEE|BUTÉE|CROCHET|GOND|GACHE|GÂCHE|ARRET|ARRÊT|VERROU|PAUMELLE|PATTE|TUYAU|KIT|MITIGEUR|FLEX|EMB|REDUCTEUR|RACCORD|PLAQUE|BA13|ENDUIT|COLLE|ROUE|COURONNE|MORTIER|CIMENT|BOIS|PANNEAU|OSB|MDF|CHEVILLE|BOULON|TUBE|SIPHON|COUDE|MANCHON|EQUERRE|RAL|FORMEL|HOX|PLASTIKA)/i
const bad=/(TOTAL|T\.V\.A|IBAN|SIRET|RCS|ADRESSE|CONDITIONS|NET A PAYER|LIVRAISON|EMAIL|TEL|TVA|MONTANT|CODE DOUANIER|EAN|CONFIRMATION DE COMMANDE|CONFIRMATION|BANQUE|CAPITAL|POIDS TOTAL|SERVICE COMMERCIAL|VOS RÉFÉRENCES|CONTACT|CONTREMARQUE|FACTURATION|SEUIL DE FRANCO|CGV|QR CODE|TAXE PARAFISCALE)/i
const normRef=r=>String(r||'').toUpperCase().replace(/[^A-Z0-9]/g,'').trim()
const normUnit=u=>{const x=String(u||'').toUpperCase().trim();return x==='PCS'||x==='UN'?'PCE':x==='BT'?'BTE':x}
const isEan=r=>/^\d{13}$/.test(normRef(r))
function qtyUnits(t){const a=[];const re=new RegExp(`(\\d+(?:[,.]\\d+)?)\\s*(${units})`,'gi');let m;while((m=re.exec(t)))a.push({qty:fmt(qte(m[1])),unit:normUnit(m[2]),raw:m[0]});return a}
function dedupe(rows){const s=new Set(),o=[];for(const r of rows){const k=normRef(r.reference)+'|'+r.quantite+'|'+(r.designation||'').slice(0,30);if(!s.has(k)){s.add(k);o.push(r)}}return o}
function extractCommandeBatiBrico(text){
 const flat=clean(text).replace(/\)\s+/g,') ')
 const rows=[]
 const starts=[...flat.matchAll(/\b[A-Z]?\d{5,12}[A-Z0-9]*\s*\(Notre réf:/gi)].map(m=>m.index)
 starts.push(flat.length)
 for(let i=0;i<starts.length-1;i++){
  const block=flat.slice(starts[i],starts[i+1]).trim()
  const h=block.match(/^([A-Z]?\d{5,12}[A-Z0-9]*)\s*\(Notre réf:\s*([A-Z0-9]+)\s+(.+)$/i)
  if(!h)continue
  const ref=h[1],refBati=h[2],rest=h[3]
  if(isEan(ref))continue
  const q=qtyUnits(rest)
  if(q.length<2)continue
  const first=rest.search(new RegExp(`\\d+(?:[,.]\\d+)?\\s*(${units})`,'i'))
  let designation=clean(first>0?rest.slice(0,first):rest).replace(/\s*\($/,'')
  if(!designation||bad.test(designation))continue
  const qCond=q[0],qCmd=q[1]
  rows.push({id:Date.now()+rows.length,reference:ref,refBati,designation,quantite:qCmd.qty,unite:qCmd.unit,conditionnement:`${qCond.raw} / ${qCmd.raw}`,source:block,confiance:'Commande BATI V6'})
 }
 return dedupe(rows)
}
function extractArcUniversel(text){
 const lines=text.replace(/\r/g,'\n').replace(/([A-Z]?\d{5,12}[A-Z0-9]*)\s+(?=[A-ZÉÈÀÂÊÎÔÛÇ])/g,'\n$1 ').split(/\n+/).map(clean).filter(l=>l.length>2)
 const rows=[],replacements=[]
 let lastArticleRef=''
 for(const line of lines){
  const rep=line.match(/Remplace\s+r[ée]f\.?\s*([A-Z0-9]+)/i)
  if(rep&&lastArticleRef){replacements.push({oldRef:normRef(rep[1]),newRef:normRef(lastArticleRef)});continue}
  if(bad.test(line))continue
  const m=line.match(/^([A-Z]?\d{5,12}[A-Z0-9]*)\s+(.+)$/i);if(!m)continue
  const ref=m[1],rest=m[2];if(isEan(ref))continue
  let qty='',unit='',conditionnement=''
  const slash=rest.match(/\b(\d+(?:[,.]\d+)?)\s*\/\s*(\d+(?:[,.]\d+)?)\b/), qu=qtyUnits(rest)
  if(slash){qty=fmt(Math.max(qte(slash[1]),qte(slash[2])));conditionnement=slash[0]}
  else if(qu.length){const x=qu[qu.length-1];qty=x.qty;unit=x.unit;conditionnement=qu.map(y=>y.raw).join(' / ')}
  else{const af=rest.match(/\s(?:0|2|4|R|T)\s+P\s+(\d+(?:[,.]\d+)?)\s+/i); if(af){qty=fmt(qte(af[1]));unit='PCE';conditionnement=af[1]} else {const nums=[...rest.matchAll(/\b\d+(?:[,.]\d+)?\b/g)].map(x=>x[0]).filter(x=>qte(x)>0&&qte(x)<5000);if(nums.length)qty=fmt(qte(nums[0]))}}
  const idx=rest.search(/\s(?:0|2|4|R|T)\s+P\s+\d|\b\d+(?:[,.]\d+)?(?:\s*\/\s*\d+(?:[,.]\d+)?)?\b/)
  let designation=clean(idx>5?rest.slice(0,idx):rest).replace(/EAN.*$/i,'').replace(/Code douanier.*$/i,'')
  let score=0;if(articleWords.test(designation+' '+rest))score+=3;if(qty)score+=2;if(ref&&!isEan(ref))score+=2;if(bad.test(rest))score-=4
  if(score<4)continue
  rows.push({id:Date.now()+rows.length,reference:ref,refBati:'',designation,quantite:qty,unite:unit,conditionnement,source:line,confiance:score>=7?'Forte':'Moyenne'})
  lastArticleRef=ref
 }
 return {rows:dedupe(rows),replacements}
}
function compareRows(cmd,arc){const out=[],used=new Set();for(const c of cmd){const idx=arc.findIndex((a,i)=>!used.has(i)&&normRef(a.reference)===normRef(c.reference));if(idx<0){out.push({status:'MANQUANT',reference:c.reference,refBati:c.refBati,designation:c.designation,message:'Article commandé absent de l’ARC / confirmation',cmd:c,arc:null});continue}used.add(idx);const a=arc[idx],cq=qte(c.quantite),aq=qte(a.quantite),cu=normUnit(c.unite),au=normUnit(a.unite);let status='OK',message='Conforme';if(cq&&aq&&Math.abs(cq-aq)>=.001){status=cu&&au&&cu===au?'ECART QTE':'A VERIFIER';message=status==='ECART QTE'?`Même unité ${cu} : commande ${c.quantite} / ARC ${a.quantite}`:`Référence trouvée, quantité/conditionnement à vérifier : commande ${c.quantite} ${cu||''} / ARC ${a.quantite} ${au||''}`}else if(cu&&au&&cu!==au){status='A VERIFIER';message=`Référence et quantité proches, mais unité différente : commande ${cu} / ARC ${au}`}out.push({status,reference:c.reference,refBati:c.refBati,designation:c.designation||a.designation,message,cmd:c,arc:a})}arc.forEach((a,i)=>{if(!used.has(i)&&normRef(a.reference))out.push({status:'SUPPLEMENTAIRE',reference:a.reference,refBati:'',designation:a.designation,message:'Article présent sur ARC mais absent de la commande',cmd:null,arc:a})});return out}
async function extractPdfText(file){const buf=await file.arrayBuffer(),pdf=await pdfjsLib.getDocument({data:buf}).promise;let full='';for(let i=1;i<=pdf.numPages;i++){const page=await pdf.getPage(i),content=await page.getTextContent();full+=content.items.map(it=>it.str).join(' ')+'\n'}return full.trim()}
async function ocrPdfPages(file,onProgress){const buf=await file.arrayBuffer(),pdf=await pdfjsLib.getDocument({data:buf}).promise;let full='';for(let i=1;i<=pdf.numPages;i++){onProgress(`OCR page ${i}/${pdf.numPages}`);const page=await pdf.getPage(i),viewport=page.getViewport({scale:2}),canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');canvas.width=viewport.width;canvas.height=viewport.height;await page.render({canvasContext:ctx,viewport}).promise;const res=await Tesseract.recognize(canvas,'fra+eng',{logger:m=>{if(m.status)onProgress(`OCR page ${i}/${pdf.numPages} — ${m.status} ${Math.round((m.progress||0)*100)}%`)}});full+=res.data.text+'\n'}return full}
async function ocrImage(file,onProgress){const res=await Tesseract.recognize(file,'fra+eng',{logger:m=>{if(m.status)onProgress(`${m.status} ${Math.round((m.progress||0)*100)}%`)}});return res.data.text}
const esc=v=>`"${String(v??'').replaceAll('"','""')}"`
export default function App(){
 const[cmdText,setCmdText]=useState(''),[arcText,setArcText]=useState(''),[cmdRows,setCmdRows]=useState([]),[arcRows,setArcRows]=useState([]),[replacements,setReplacements]=useState([]),[tab,setTab]=useState('import'),[status,setStatus]=useState(''),[loading,setLoading]=useState(false)
 const results=useMemo(()=>compareRows(cmdRows,arcRows,replacements),[cmdRows,arcRows,replacements]),ok=results.filter(r=>r.status==='OK').length,rep=results.filter(r=>r.status==='REMPLACEMENT').length,ec=results.filter(r=>r.status==='ECART QTE').length,verif=results.filter(r=>r.status==='A VERIFIER').length,man=results.filter(r=>r.status==='MANQUANT').length,sup=results.filter(r=>r.status==='SUPPLEMENTAIRE').length
 const importFile=async(file,target)=>{if(!file)return;setLoading(true);setStatus(`Lecture de ${file.name}...`);try{let text='';if(file.type==='application/pdf'||file.name.toLowerCase().endsWith('.pdf')){text=await extractPdfText(file);if(!text||text.length<50){setStatus('PDF scanné détecté : OCR automatique page par page...');text=await ocrPdfPages(file,setStatus)}else setStatus('Texte PDF extrait automatiquement.')}else if(file.type.startsWith('image/')){text=await ocrImage(file,setStatus);setStatus('OCR image terminé.')}else{text=await file.text();setStatus('Texte importé.')}target==='cmd'?setCmdText(text):setArcText(text)}catch(e){setStatus('Extraction impossible : colle le texte OCR manuellement.')}setLoading(false)}
 const parseCmd=()=>{const r=extractCommandeBatiBrico(cmdText);setCmdRows(r);setStatus(`${r.length} ligne(s) commande BATI BRICO détectée(s).`);setTab('validation')},parseArc=()=>{const r=extractArcUniversel(arcText);setArcRows(r.rows);setReplacements(r.replacements);setStatus(`${r.rows.length} ligne(s) ARC / confirmation détectée(s), ${r.replacements.length} remplacement(s).`);setTab('validation')},parseBoth=()=>{const c=extractCommandeBatiBrico(cmdText),a=extractArcUniversel(arcText);setCmdRows(c);setArcRows(a.rows);setReplacements(a.replacements);setStatus(`${c.length} ligne(s) commande, ${a.rows.length} ligne(s) ARC, ${a.replacements.length} remplacement(s).`);setTab('controle')}
 const upd=(type,id,k,v)=>(type==='cmd'?setCmdRows:setArcRows)(rows=>rows.map(r=>r.id===id?{...r,[k]:v}:r)),add=(type)=>(type==='cmd'?setCmdRows:setArcRows)(rows=>[...rows,{id:Date.now(),reference:'',refBati:'',designation:'',quantite:'',unite:'',conditionnement:'',source:'',confiance:'Manuel'}]),del=(type,id)=>(type==='cmd'?setCmdRows:setArcRows)(rows=>rows.filter(r=>r.id!==id))
 const exportCSV=kind=>{let rows,fn;if(kind==='rapport'){rows=[['Statut','Réf fournisseur','Réf BATI','Désignation','Message','Qté commande','Unité commande','Qté ARC','Unité ARC'],...results.map(r=>[r.status,r.reference,r.refBati,r.designation,r.message,r.cmd?.quantite||'',r.cmd?.unite||'',r.arc?.quantite||'',r.arc?.unite||''])];fn='rapport_controle_arc_bati_brico.csv'}else{const s=kind==='cmd'?cmdRows:arcRows;rows=[['Réf fournisseur','Réf BATI','Désignation','Quantité','Unité','Conditionnement','Confiance','Source'],...s.map(r=>[r.reference,r.refBati,r.designation,r.quantite,r.unite,r.conditionnement,r.confiance,r.source])];fn=kind==='cmd'?'commande_bati_brico_normalisee.csv':'arc_fournisseur_normalise.csv'}const b=new Blob([rows.map(r=>r.map(esc).join(';')).join('\n')],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=fn;a.click()}
 const Editor=({title,type,rows})=><section className="card wide"><div className="head"><h2>{title}</h2><button onClick={()=>add(type)}><Plus size={16}/>Ajouter ligne</button></div><div className="table"><table><thead><tr><th>Réf fournisseur</th><th>Réf BATI</th><th>Désignation</th><th>Qté</th><th>Unité</th><th>Conditionnement</th><th>Confiance</th><th></th></tr></thead><tbody>{rows.length===0&&<tr><td colSpan="8" className="empty">Aucune ligne détectée</td></tr>}{rows.map(r=><tr key={r.id}><td><input value={r.reference} onChange={e=>upd(type,r.id,'reference',e.target.value)}/></td><td><input value={r.refBati||''} onChange={e=>upd(type,r.id,'refBati',e.target.value)}/></td><td><input value={r.designation} onChange={e=>upd(type,r.id,'designation',e.target.value)}/></td><td><input value={r.quantite} onChange={e=>upd(type,r.id,'quantite',e.target.value)}/></td><td><input value={r.unite||''} onChange={e=>upd(type,r.id,'unite',e.target.value)}/></td><td><input value={r.conditionnement||''} onChange={e=>upd(type,r.id,'conditionnement',e.target.value)}/></td><td>{r.confiance}</td><td><button className="danger mini" onClick={()=>del(type,r.id)}><Trash2 size={14}/></button></td></tr>)}</tbody></table></div></section>
 return <div className="app"><header><div><h1>BATI BRICO</h1><p>Contrôle ARC fournisseur V7 — BATI multi-pages + remplacements</p></div><div className="status"><span>Contrôle</span><b>{results.length?`${ok} OK / ${rep} remplacement / ${verif} à vérifier / ${ec+man+sup} alerte(s)`:'En attente'}</b></div></header><nav><button className={tab==='import'?'active':''}onClick={()=>setTab('import')}>1. Import</button><button className={tab==='validation'?'active':''}onClick={()=>setTab('validation')}>2. Validation</button><button className={tab==='controle'?'active':''}onClick={()=>setTab('controle')}>3. Contrôle</button></nav>{tab==='import'&&<main className="grid"><section className="card"><h2><Upload/>Commande BATI BRICO</h2><p>Analyse par blocs autour de “Notre réf”.</p><input type="file" accept=".pdf,.txt,.csv,image/*" onChange={e=>importFile(e.target.files[0],'cmd')}/><textarea value={cmdText} onChange={e=>setCmdText(e.target.value)} placeholder="Texte commande extrait automatiquement ou collé ici..."/><button onClick={parseCmd}><Wand2 size={16}/>Extraire commande BATI</button></section><section className="card"><h2><FileText/>ARC / confirmation fournisseur</h2><p>PDF, scan, image ou confirmation fournisseur.</p><input type="file" accept=".pdf,.txt,.csv,image/*" onChange={e=>importFile(e.target.files[0],'arc')}/><textarea value={arcText} onChange={e=>setArcText(e.target.value)} placeholder="Texte ARC extrait automatiquement ou collé ici..."/><button onClick={parseArc}><Wand2 size={16}/>Extraire ARC</button></section><section className="card wide notice">{loading?<Loader2 className="spin"/>:<ScanText/>}<b>État :</b> {status||'En attente d’import.'}<br/>V7 : commande multi-pages + détection “Remplace réf”.<br/><button className="compare" onClick={parseBoth}><RefreshCw size={16}/>Extraire les 2 documents et comparer</button></section></main>}{tab==='validation'&&<main><div className="actions"><button onClick={()=>exportCSV('cmd')}><Download/>Export commande</button><button onClick={()=>exportCSV('arc')}><Download/>Export ARC</button><button onClick={()=>setTab('controle')}><CheckCircle/>Comparer</button></div><Editor title={`Commande BATI BRICO normalisée (${cmdRows.length})`} type="cmd" rows={cmdRows}/><Editor title={`ARC / confirmation fournisseur normalisé (${arcRows.length})`} type="arc" rows={arcRows}/>{replacements.length>0&&<section className="card wide"><h2><Repeat2/>Remplacements détectés</h2>{replacements.map((r,i)=><p key={i}>{r.oldRef} → {r.newRef}</p>)}</section>}</main>}{tab==='controle'&&<main><section className="summary"><div className="ok"><CheckCircle/><b>{ok}</b><span>Conformes</span></div><div className="replace"><Repeat2/><b>{rep}</b><span>Remplacements</span></div><div className="check"><HelpCircle/><b>{verif}</b><span>À vérifier</span></div><div className="warn"><AlertTriangle/><b>{ec}</b><span>Écarts quantité</span></div><div className="alert"><AlertTriangle/><b>{man}</b><span>Manquants</span></div><div className="extra"><AlertTriangle/><b>{sup}</b><span>Supplémentaires</span></div></section><div className="actions"><button onClick={()=>exportCSV('rapport')}><Download/>Export rapport CSV</button><button onClick={()=>window.print()}><FileText/>Imprimer</button></div><section className="card wide"><h2>Rapport conformité ARC / Commande</h2><div className="table"><table><thead><tr><th>Statut</th><th>Réf fournisseur</th><th>Réf BATI</th><th>Désignation</th><th>Détail</th><th>Qté commande</th><th>Unité cmd</th><th>Qté ARC</th><th>Unité ARC</th></tr></thead><tbody>{results.length===0&&<tr><td colSpan="9" className="empty">Aucun contrôle</td></tr>}{results.map((r,i)=><tr key={i} className={r.status.toLowerCase().replaceAll(' ','-')}><td><b>{r.status}</b></td><td>{r.reference}</td><td>{r.refBati}</td><td>{r.designation}</td><td>{r.message}</td><td>{r.cmd?.quantite||''}</td><td>{r.cmd?.unite||''}</td><td>{r.arc?.quantite||''}</td><td>{r.arc?.unite||''}</td></tr>)}</tbody></table></div></section></main>}</div>
}
