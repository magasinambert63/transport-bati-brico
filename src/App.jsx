import React,{useEffect,useMemo,useState}from'react'
import{MapPin,Truck,Calculator,Copy,AlertTriangle,CheckCircle,Loader2,Euro,Clock,Route,Printer,Download,Trash2,History,Home,ShieldCheck}from'lucide-react'
const DEPOTS=[{id:'ambert',nom:'BATI BRICO Ambert',lat:45.5507,lon:3.7419},{id:'arlanc',nom:'BATI BRICO Arlanc',lat:45.4109,lon:3.7254}]
const ZONES=[{zone:'Zone 1',tarif:32,max:32},{zone:'Zone 2',tarif:41,max:41},{zone:'Zone 3',tarif:51,max:51},{zone:'Zone 4',tarif:60,max:60},{zone:'Zone 5',tarif:70,max:70},{zone:'Zone 6',tarif:80,max:80},{zone:'Zone 7',tarif:90,max:90},{zone:'Zone 8',tarif:117,max:117}]
const COST_KM=0.77,COST_HOUR=16,LOAD_MIN=30,UNLOAD_MIN=30,TVA=0.20,HISTORY_KEY='bati_brico_transport_history_v2'
const euro=n=>`${Number(n||0).toFixed(2).replace('.',',')} €`;const km=n=>`${Number(n||0).toFixed(1).replace('.',',')} km`
function minutesToText(min){const h=Math.floor(min/60),m=Math.round(min%60);if(h<=0)return`${m} min`;if(m===0)return`${h} h`;return`${h} h ${String(m).padStart(2,'0')}`}
function getZone(cost){return ZONES.find(z=>cost<=z.max)||{zone:'Sur devis',tarif:null,max:null}}
function getProfitStatus(r){if(!r?.zone?.tarif)return{label:'Sur devis',className:'red',text:'Coût supérieur au barème : devis conseillé.'};const d=r.zone.tarif-r.cost;if(d>=10)return{label:'Rentable',className:'green',text:`Marge de sécurité confortable : ${euro(d)} HT.`};if(d>=0)return{label:'Limite',className:'orange',text:`Livraison proche de l’équilibre : ${euro(d)} HT.`};return{label:'À perte',className:'red',text:`Déficit estimé : ${euro(d)} HT.`}}
function resultText(r,address,client){if(!r)return'';const t=r.zone.tarif,s=getProfitStatus(r);return['ASSISTANT TRANSPORT BATI BRICO','',client?`Client / chantier : ${client}`:'',`Adresse : ${address}`,`Dépôt retenu : ${r.depot.nom}`,`Distance A/R : ${km(r.distanceAR)}`,`Temps route A/R : ${minutesToText(r.durationAR)}`,`Temps total : ${minutesToText(r.totalMinutes)}`,`Coût estimé : ${euro(r.cost)} HT`,`Zone conseillée : ${r.zone.zone}`,t?`Tarif : ${euro(t)} HT / ${euro(t*(1+TVA))} TTC`:'Tarif : sur devis',t?`Écart estimé : ${euro(t-r.cost)} HT`:'',`Statut : ${s.label}`].filter(Boolean).join('\n')}
async function geocodeAddress(address){const url=`https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=fr&q=${encodeURIComponent(address)}`;const res=await fetch(url,{headers:{Accept:'application/json'}});if(!res.ok)throw new Error('Erreur géolocalisation');const data=await res.json();if(!data?.length)throw new Error('Adresse introuvable');return{lat:Number(data[0].lat),lon:Number(data[0].lon),display:data[0].display_name}}
async function routeFromDepot(depot,dest){const url=`https://router.project-osrm.org/route/v1/driving/${depot.lon},${depot.lat};${dest.lon},${dest.lat}?overview=false&alternatives=false&steps=false`;const res=await fetch(url);if(!res.ok)throw new Error('Erreur calcul itinéraire');const data=await res.json();if(data.code!=='Ok'||!data.routes?.length)throw new Error('Aucun itinéraire trouvé');const route=data.routes[0],distanceOneWay=route.distance/1000,durationOneWay=route.duration/60,distanceAR=distanceOneWay*2,durationAR=durationOneWay*2,totalMinutes=durationAR+LOAD_MIN+UNLOAD_MIN,cost=distanceAR*COST_KM+(totalMinutes/60)*COST_HOUR,zone=getZone(cost);return{depot,distanceOneWay,durationOneWay,distanceAR,durationAR,totalMinutes,cost,zone}}
export default function App(){const[client,setClient]=useState(''),[address,setAddress]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState(''),[result,setResult]=useState(null),[geo,setGeo]=useState(null),[allRoutes,setAllRoutes]=useState([]),[copied,setCopied]=useState(false),[history,setHistory]=useState([]);useEffect(()=>{try{setHistory(JSON.parse(localStorage.getItem(HISTORY_KEY)||'[]'))}catch(e){setHistory([])}},[]);const status=useMemo(()=>getProfitStatus(result),[result]);const saveHistory=item=>{const next=[item,...history.filter(h=>h.id!==item.id)].slice(0,30);setHistory(next);localStorage.setItem(HISTORY_KEY,JSON.stringify(next))};const calculate=async()=>{setError('');setResult(null);setAllRoutes([]);setCopied(false);if(!address.trim()){setError('Renseigne une adresse complète ou au minimum une commune.');return}setLoading(true);try{const destination=await geocodeAddress(address);setGeo(destination);const routes=await Promise.all(DEPOTS.map(d=>routeFromDepot(d,destination)));routes.sort((a,b)=>a.cost-b.cost);const best=routes[0];setAllRoutes(routes);setResult(best);saveHistory({id:Date.now(),date:new Date().toLocaleString('fr-FR'),client,address:destination.display,shortAddress:address,depot:best.depot.nom,zone:best.zone.zone,tarif:best.zone.tarif,cost:best.cost,distanceAR:best.distanceAR,durationAR:best.durationAR,totalMinutes:best.totalMinutes,status:getProfitStatus(best).label})}catch(e){setError(e.message||'Erreur pendant le calcul.')}finally{setLoading(false)}};const copyResult=async()=>{if(!result)return;await navigator.clipboard.writeText(resultText(result,geo?.display||address,client));setCopied(true)};const exportCSV=()=>{const rows=[['Date','Client','Adresse','Dépôt','Zone','Tarif HT','Coût estimé','Écart HT','Distance A/R','Temps route A/R','Temps total','Statut'],...history.map(h=>[h.date,h.client,h.address,h.depot,h.zone,h.tarif??'Sur devis',h.cost,h.tarif?Number(h.tarif-h.cost).toFixed(2):'',h.distanceAR.toFixed(1),minutesToText(h.durationAR),minutesToText(h.totalMinutes),h.status])];const csv=rows.map(r=>r.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(';')).join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='historique_transport_bati_brico.csv';a.click()};const clearHistory=()=>{if(!confirm('Supprimer tout l’historique local ?'))return;setHistory([]);localStorage.removeItem(HISTORY_KEY)};const loadHistory=h=>{setClient(h.client||'');setAddress(h.shortAddress||h.address||'');window.scrollTo({top:0,behavior:'smooth'})};return <div className="app"><header><div><h1>BATI BRICO</h1><p>Assistant Transport — zone, coût réel et rentabilité</p></div><div className="pill"><Truck size={22}/><div><span>Coût base</span><b>0,77 €/km + 16 €/h</b></div></div></header><main className="grid"><section className="card"><h2><MapPin/>Adresse de livraison</h2><label>Nom client / chantier</label><input value={client} onChange={e=>setClient(e.target.value)} placeholder="Ex : Dupont / Chantier Martin"/><label>Adresse complète</label><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="Ex : Place de la mairie 63880 Olliergues"/><button className="primary" onClick={calculate} disabled={loading}>{loading?<Loader2 className="spin"/>:<Calculator/>}Calculer zone transport</button>{error&&<div className="error"><AlertTriangle/> {error}</div>}<div className="info"><b>Temps ajouté automatiquement :</b><br/>30 min chargement + 30 min déchargement.</div></section><section className="card"><h2><Euro/>Barème zones HT</h2><table><thead><tr><th>Zone</th><th>Tarif HT</th><th>TTC</th></tr></thead><tbody>{ZONES.map(z=><tr key={z.zone}><td>{z.zone}</td><td>{euro(z.tarif)}</td><td>{euro(z.tarif*(1+TVA))}</td></tr>)}</tbody></table><div className="info">Coût estimé supérieur à 117 € HT : <b>sur devis</b>.</div></section>{result&&<section className="card wide result printArea"><div className="resultHead"><div><h2><CheckCircle/>Résultat transport</h2><p>{geo?.display}</p></div><div className="buttons"><button onClick={copyResult}><Copy/> {copied?'Copié':'Copier'}</button><button onClick={()=>window.print()}><Printer/> PDF / Imprimer</button></div></div><div className={`statusBox ${status.className}`}><ShieldCheck/><div><b>{status.label}</b><span>{status.text}</span></div></div><div className="score"><div className="zoneBox"><span>Zone conseillée</span><b>{result.zone.zone}</b><small>{result.zone.tarif?`${euro(result.zone.tarif)} HT / ${euro(result.zone.tarif*(1+TVA))} TTC`:'Sur devis'}</small></div><div><Home/><span>Dépôt retenu</span><b>{result.depot.nom.replace('BATI BRICO ','')}</b></div><div><Route/><span>Distance A/R</span><b>{km(result.distanceAR)}</b></div><div><Clock/><span>Temps total</span><b>{minutesToText(result.totalMinutes)}</b></div><div><Euro/><span>Coût estimé</span><b>{euro(result.cost)}</b></div></div><h3>Comparaison des dépôts</h3><table><thead><tr><th>Dépôt</th><th>Distance A/R</th><th>Temps route A/R</th><th>Temps total</th><th>Coût estimé</th></tr></thead><tbody>{allRoutes.map(r=><tr key={r.depot.id} className={r.depot.id===result.depot.id?'selected':''}><td>{r.depot.nom}</td><td>{km(r.distanceAR)}</td><td>{minutesToText(r.durationAR)}</td><td>{minutesToText(r.totalMinutes)}</td><td>{euro(r.cost)}</td></tr>)}</tbody></table></section>}<section className="card wide noPrint"><div className="resultHead"><h2><History/>Historique local</h2><div className="buttons"><button onClick={exportCSV}><Download/> Export CSV</button><button className="danger" onClick={clearHistory}><Trash2/> Vider</button></div></div><table><thead><tr><th>Date</th><th>Client</th><th>Adresse</th><th>Dépôt</th><th>Zone</th><th>Coût</th><th>Statut</th><th></th></tr></thead><tbody>{history.length===0&&<tr><td colSpan="8" className="empty">Aucun calcul enregistré pour le moment.</td></tr>}{history.map(h=><tr key={h.id}><td>{h.date}</td><td>{h.client}</td><td>{h.shortAddress||h.address}</td><td>{h.depot.replace('BATI BRICO ','')}</td><td>{h.zone}</td><td>{euro(h.cost)}</td><td>{h.status}</td><td><button className="mini" onClick={()=>loadHistory(h)}>Recharger</button></td></tr>)}</tbody></table></section></main></div>}
